import pandas as pd
import numpy as np
import random
from datetime import datetime, timedelta

# Configuración de la simulación
n_animales = 500
categorias = {
    "Ternero": {"peso_mu": 180, "peso_sigma": 20, "gmd": 0.720},
    "Novillito": {"peso_mu": 320, "peso_sigma": 35, "gmd": 0.850},
    "Novillo": {"peso_mu": 430, "peso_sigma": 40, "gmd": 0.950}
}

data = []
for i in range(n_animales):
    # Elegimos categoría al azar
    cat = random.choice(list(categorias.keys()))
    config = categorias[cat]
    
    # Generamos peso con distribución normal
    peso = round(np.random.normal(config["peso_mu"], config["peso_sigma"]), 1)
    
    # Inyectamos anomalías (Outliers) cada 40 animales
    if i % 40 == 0:
        peso = peso * 0.6  # Anomalía negativa (Enfermo/Quedado)
    
    caravana = f"320000{random.randint(100000000, 999999999)}"
    lote = f"Lote_{cat}_{random.choice(['Norte', 'Sur', 'Bajo'])}"
    
    data.append({
        "Caravana": caravana,
        "Peso_Actual": peso,
        "Fecha_Pesada": (datetime.now() - timedelta(days=random.randint(0, 15))).strftime('%Y-%m-%d'),
        "GMD_Esperada": config["gmd"],
        "Lote": lote,
        "Categoria": cat
    })

df_simulado = pd.DataFrame(data)
df_simulado.to_csv("datos_prueba_gi.csv", index=False)
print("✅ Archivo 'datos_prueba_gi.csv' generado con 500 animales.")